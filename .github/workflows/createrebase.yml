name: "Create rebased branch"

on:
  pull_request:
    branches:
      - develop  
    types:
      - assigned
      - synchronize

jobs:
    call-createrebase:
      uses: itext/github-workflows/.github/workflows/createrebase.yml@master
      secrets: inherit