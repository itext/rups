name: Test

on:
  create:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Get email
        id: get-email
        uses: evvanErb/get-github-email-by-username-action@v2.0
        with:
          github-username: marco.andries@apryse.com
          token: ${{ secrets.GH_TOKEN }}
