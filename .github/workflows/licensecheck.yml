name: LicenseCheck

on:
  push:
    # 'branches-ignore' or 'branches' can be used to filter specific branches.
    # By default, without any filters, it runs on every push to all branches.
    # To be explicit, you can use:
    branches-ignore:
      - 'develop'
      - 'master'
      - 'rebased/*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
         fetch-depth: 1
      
      - name: Get changes
        id: changed-files
        run: |
            echo "Changed files from ${{ github.event.before }} > ${{ github.event.after }}"
            BEFORE_HASH="${{ github.event.before }}"
            AFTER_HASH="${{ github.event.after }}"
            if BEFORE_HASH == "000000000000000000000"; then
                git fetch origin develop
                echo "changed_files=$(git diff --name-only remotes/origin/develop $AFTER_HASH | xargs)" >> $GITHUB_OUTPUT
            else
                echo "changed_files=$(git diff --name-only $BEFORE_HASH $AFTER_HASH | xargs)" >> $GITHUB_OUTPUT
            fi

      - name: Process changed files
        id: license-lines
        run: |
            LICENSE_LINES=''
            for file in ${{ steps.changed-files.outputs.changed_files }}; do
              echo "$file was changed"
              LICENSE_LINE="<$file>
              $(cat $file | grep -Pzo '(<|")licensee("| )(\n|.)*(}|</licensee>)' | xargs)
              "
              LICENSE_LINES="$LICENSE_LINES
              $LICENSE_LINE"
            done
            {
                echo 'license_lines<<EOF'
                echo "${LICENSE_LINES}"
                echo EOF
            } >> $GITHUB_OUTPUT

      - name: List licenses found
        run: echo "${{ steps.license-lines.outputs.license_lines }}"
